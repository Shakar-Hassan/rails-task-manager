<h1><%= @task.title %></h1>
<%= link_to "Delete",
tasks_path(@task),
data: {tourbo_method: :delete, tourbo_confirm: "Are you sure?"} %>

<p> <%= @task.details %> </p>

<%= link_to "Back to list", tasks_path %>
